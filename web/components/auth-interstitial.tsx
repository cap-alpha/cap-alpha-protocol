"use client";

// AuthInterstitial — intentionally disabled for public launch.
// The site is free to browse; sign-in is opt-in via the navbar button.
// Re-enable this component only after a deliberate product decision to gate
// specific features behind login.

export function AuthInterstitial() {
    return null;
}
